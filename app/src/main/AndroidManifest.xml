<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.buylist">

    <application
        android:name=".utils.BuylistApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".buy_list.BuyListActivity"
            android:parentActivityName=".collection_lists.CollectionActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".pattern_list.PatternListActivity"
            android:parentActivityName=".collection_lists.CollectionActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".recipe_list.RecipeListActivity"
            android:parentActivityName=".collection_lists.CollectionActivity"
            android:windowSoftInputMode="adjustPan" />

        <activity
            android:name=".collection_lists.CollectionActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>